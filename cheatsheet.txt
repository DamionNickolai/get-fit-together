Run dev app:
python -m streamlit run get_fit_together.py

Change to dev env:
git checkout dev
git switch

Change to main env:
git checkout main
git switch

close dev app:
Ctrl + c

quick save:
Ctrl + s

AUTO PUSH FROM LOCAL TO CLOUD (Change Message):
python deploy.py "commit message"

Push a small fix to dev staging:
git add auth.py
git commit --amend --no-edit
git push --force origin dev

ENTER VIRTUAL ENV:
.\venv\Scripts\activate